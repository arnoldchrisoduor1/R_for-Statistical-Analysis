#we use the paste() function to join elements.

text <- "awesome"
new <- paste("R is", text)
cat(new, "\n")

text2 <- "R is"
newer <- paste(text2, text)
print(newer)

#Math operators
num1 <- 5
num2 <- 10
print(num1 + num2)

#combining a number and a string will give an error.